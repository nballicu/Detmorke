/* Reset dei margini e dei padding */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Stile di base per il body */
body {
    font-family: Arial, sans-serif;
    line-height: 1.6;
}

/* Stile per l'header */
header {
    background-color: #333;
    color: #fff;
    padding: 20px;
    text-align: center;
}

header h1 {
    font-size: 30px;
}

nav ul {
    list-style: none;
    margin-top: 20px;
}

nav ul li {
    display: inline-block;
    margin: 0 10px;
}

nav ul li a {
    color: #fff;
    text-decoration: none;
}

/* Stile per la sezione home */
.hero-image {
    background-image: url('band-image.jpg');
    background-size: cover;
    background-position: center;
    height: 500px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
}

.hero-image h2 {
    color: #fff;
    font-size: 36px;
    margin-bottom: 20px;
}

.btn {
    background-color: #333;
    color: #fff;
    padding: 10px 20px;
    border: none;
    text-decoration: none;
    font-size: 16px;
    cursor: pointer;
}

.btn:hover {
    background-color: #666;
}

/* Stile per le altre sezioni */
section {
    padding: 50px;
}

section h2 {
    font-size: 24px;
    margin-bottom: 20px;
}

/* Stile per la sezione music */
.music-container {
    display: flex;
    flex-wrap: wrap;
}

.song {
    width: 50%;
    margin-bottom: 20px;
}

/* Stile per il footer */
footer {
    background-color: #333;
    color: #fff;
    padding: 20px;
    text-align: center;
}
